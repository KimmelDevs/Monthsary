rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections (no auth — personal private app)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
